(function (a) { a.switcher = function (c) { let b = a('input[type=checkbox],input[type=radio]'); if (c !== undefined && c.length) { b = b.filter(c); }b.each(function () { const e = a(this).hide(); const d = a(document.createElement('div')).addClass('ui-switcher').attr('aria-checked', e.is(':checked')); if (e.attr('type') === 'radio') { d.attr('data-name', e.attr('name')); }toggleSwitch = function (f) { if (f.target.type === undefined) { e.trigger(f.type); }d.attr('aria-checked', e.is(':checked')); if (e.attr('type') === 'radio') { a(`.ui-switcher[data-name=${e.attr('name')}]`).not(d.get(0)).attr('aria-checked', false); } }; d.on('click', toggleSwitch); e.on('click', toggleSwitch); d.insertBefore(e); }); }; }(jQuery));
