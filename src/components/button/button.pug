mixin button({type = 'button', size = 'big', color = 'green', ripple = true, text = 'Click me'} = {})
  button(type = type, class = `button button_${size} button_${color} ${ripple ? ' button_ripple' : ''}`) #{text}
