include components/buttons/standard/button
include components/buttons/arrow/button
include components/progress/slider/slider
include components/bubble/bubble

doctype html

hmtl(lang='en')
  head
    meta(charset='utf-8')
    title UI page
  body
    #content
      h2 STANDARD BUTTONS
      #standard-buttons
        +standard_button('small-green-button') small green
        +standard_button('big-green-button') big green
        +standard_button('big-orange-button') big orange
        +standard_button('small-orange-button') small orange
      
      h2 ARROW BUTTONS
      #arrow-buttons
        +arrow_button('left-arrow')
        +arrow_button('right-arrow')

      h2 PERCENTAGES/PIE CHART
      #jtest

      h2 SLIDERS
      +slider('slider', '50')

      div
        +bubble('top-bubble')
          h2 WEIRD
        +bubble('right-bubble', {width:'100px', height:'100px'})

      h2 STAGES
      div(id="bluecircle", data-percent="17", class="big")

      h2 FORM ELEMENTS
      
      #test
        
